/**
 * Converts a hex string into rgba array.
 *
 * @param hexString The hex string.
 * @example hexToRgb('ffffff') == [0, 0, 0, 1]
 */
export declare function hexToRgb(hexString: string): [number, number, number, number];
